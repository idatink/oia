{
  "name": "@nia/whatsapp",
  "version": "0.1.0",
  "private": true,
  "description": "OpenClaw-based WhatsApp intake gateway for Nia",
  "scripts": {
    "dev": "openclaw gateway --config ./openclaw.json",
    "deploy": "fly deploy"
  }
}
